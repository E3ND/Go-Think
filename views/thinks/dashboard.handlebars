<div class="dashboard">
    <div class="title-container">
        <h2>Dashboard</h2>
        <a href="/thinks/add" class="btn">Criar pensamentos</a>
    </div>
    <h3>Seus pensamentos</h3>
    <ul>
        {{#each thinks}}
            <li>
                <span class="label">{{this.title}}</span>
                <span class="actions">
                    <a href="/thinks/edit/{{this.id}}" class="btn">Editar</a>
                    <form action="/thinks/remove" method="POST">
                        <input type="hidden" name="id" value="{{this.id}}">
                        <input type="submit" value="Excluir" class="btn">
                    </form>
                </span>
            </li>
        {{/each}}
    </ul>
    {{#if emptyThinks}}
        <p>Ainda não há nenhum pensamento seu, comece a criar <a href="/thinks/add">por aqui!</a></p>
    {{/if}}
</div>